@model IEnumerable<DataLayer.Model.Blog>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Blog";
    ViewData["ButtonName"] = "Add New Blog";
    ViewData["Reads"] = "Reads";
    ViewData["TableName"] = "Blog Name";

}
@Html.Partial("_AdminSharedPartialUp")
    @foreach (var blog in Model)
    {
    <tr>
        <td>@blog.Id</td>
        <td>@blog.Title</td>
        <td>@blog.CreatedDate</td>
        <td class="project_progress">@blog.Description</td>
        <td class="project-state">username</td>
        <td class="project-actions text-right">
            <a class="btn btn-primary btn-sm" href="@Url.Action("View", "Admin", new { id = blog.Id })">
                <i class="fas fa-folder">
                </i>
                View
            </a>
            <a class="btn btn-info btn-sm" href="@Url.Action("Edit", "Admin", new { id = blog.Id })">
                <i class="fas fa-pencil-alt">
                </i>
                Edit
            </a>
            <a class="btn btn-danger btn-sm" href="@Url.Action("Delete", "Admin", new { id = blog.Id })">
                <i class="fas fa-trash">
                </i>
                Delete
            </a>
        </td>
    </tr>
    }
@Html.Partial("_AdminSharedPartialDown")